# .env

VCP_Key=Your_Secret_VCP_Key_Here

# --- Gotify Push Notification (Optional) ---

# Set to "true" to enable, or "false" (or leave empty/commented) to disable.
# If enabled, Gotify_Url and Gotify_App_Token must also be configured below.
Enable_Gotify_Push=false

# Example: Gotify_Url=http://localhost:8080
# Example: Gotify_Url=https://gotify.yourdomain.com
Gotify_Url=

# The application token generated in Gotify for this application/plugin.
# Required if Enable_Gotify_Push is true.
Gotify_App_Token=

# (Optional) Default priority for Gotify messages (integer).
# Refer to Gotify documentation for priority levels (e.g., 0-10).
# If not set, a default priority (e.g., 2) will be used by the plugin.
Gotify_Priority=0

# --- General Plugin Debug Mode (Optional) ---
# This plugin also respects the global DebugMode setting in the main config.env.
# If you want to set a specific debug mode for ONLY this plugin (which might override global if plugin code prefers it),
# you can uncomment and set it here. Typically, managing DebugMode globally is simpler.
# DebugMode=false 