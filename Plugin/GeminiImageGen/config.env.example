# Gemini Image Generation Plugin Configuration

# 在此处填入您的 Gemini API 密钥，可以填入多个，用英文逗号隔开。
# 插件会从中随机选择一个使用。
GeminiImageKey=key1,key2,key3

# 如果您需要通过代理访问 Gemini API，请在此处设置您的代理地址。
# 例如：http://127.0.0.1:7890
# 如果不需要代理，请将此行留空或注释掉。
GeminiImageProxy=
# 分布式图床服务器地址列表，用于降级处理 file:// URL。
# 格式: http://<ip>:<port>/pw=<key> (多个地址用逗号隔开)
# 插件会尝试将 file:// 路径的文件名拼接到这个地址后面进行访问。
# 例如: http://192.168.0.1:5974/pw=123456
DIST_IMAGE_SERVERS=