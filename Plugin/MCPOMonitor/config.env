# MCPOMonitor 插件配置文件
# MCPO 服务状态监控器配置

# MCPO 服务器连接配置
# 注意：如果与MCPO插件共存，优先使用MCPO插件的端口配置
# 可以只设置MCPO_PORT，或者设置完整的MCPO_HOST URL
MCPO_PORT=9000
MCPO_HOST=http://0.0.0.0:9000
MCPO_API_KEY=vcp-mcpo-secret

# 缓存配置
ENABLE_CACHE=true
CACHE_TTL_MINUTES=2

# 显示配置
INCLUDE_DETAILED_PARAMS=true

# 健康检查配置
HEALTH_CHECK_TIMEOUT=5000

# 更新间隔配置（Cron表达式）
# 默认每10秒更新一次：*/10 * * * * *
# 每次检测都会实时获取最新状态，确保占位符{{MCPOServiceStatus}}显示实时信息
# 其他示例：
# 每5秒: */5 * * * * *
# 每10秒: */10 * * * * *
# 每30秒: */30 * * * * *
# 每分钟: 0 * * * * *
# 每5分钟: 0 */5 * * * *
# 每10分钟: 0 */10 * * * *
# 每小时: 0 0 * * * *
# 修改后需要重启VCP服务器才能生效：pm2 restart server
REFRESH_INTERVAL_CRON=*/10 * * * * *

# 调试模式
# DebugMode=true